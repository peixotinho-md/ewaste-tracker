# Dependências de desenvolvimento — NÃO são necessárias para rodar o sistema.
#
# Quem só quer subir o servidor instala `requirements.txt`, que continua tendo
# apenas o que o e-Trilha MS usa em execução. Quem vai mexer no código instala
# os dois:
#
#   python -m pip install -r requirements.txt -r requirements-dev.txt
#   python -m pytest

-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
